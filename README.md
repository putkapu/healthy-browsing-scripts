# X (Twitter) — Following only + Recent (Tampermonkey)

Forces X Home feed to:
- Following only
- Recent (chronological, `?f=live`)
- Hides "For you" and the tab header UI

## Install
1. Install Tampermonkey (Chrome)
2. Open this script file:
   - `x-following-only.user.js`
3. Tampermonkey will prompt you to install it

## Notes
X changes its UI often. If it breaks, open an issue with screenshots / DOM snippet.

